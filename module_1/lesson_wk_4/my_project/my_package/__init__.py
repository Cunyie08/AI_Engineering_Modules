# __init__.py
print("my _package is being imported")

# Optional: import functions directly for easier access
from my_package.math_utils import add, subtract
from my_package.string_utils import capitalize_text, greet, reverse_text